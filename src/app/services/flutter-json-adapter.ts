import { WidgetModel } from "../models/widget.model";

export class FlutterJsonAdapter {
    generateFlutterJSON(elements:Array<WidgetModel>):string{
        console.log(elements);
        return '';
    }
}